<cu-custom bgImage="https://image.weilanwl.com/color2.0/plugin/cjkz2329.jpg">
    <view slot="content" mode="widthFix">分类
    </view>
  </cu-custom>
<scroll-view scroll-y class="scrollPage">
  <view class="cu-card">
    <view class="cu-item bg-img shadow-blur" style="background-image:url({{item.programImageUrl}})" bindtap="toCategoryArticleList" data-category="{{item}}" wx:for="{{contentCategoryList}}" wx:key>
      <view class="cardTitle">
        {{item.categoryName}}
      </view>
    </view>
  </view>
  <!-- <view class="cu-card">
    <view class="cu-item bg-img shadow-blur" style="background-image:url({{item.img}})" bindtap="toChild" data-url="{{item.url}}" wx:for="{{list}}" wx:key>
      <view class="cardTitle">
        {{item.title}}
      </view>
    </view>
  </view> -->
  <view class="cu-tabbar-height"></view>
</scroll-view>